# Behavioral Economics: Cable Car Simulation ![Status](https://img.shields.io/badge/status-complete-brightgreen)

This assignment was completed for the course **Behavioral Economics: Learning and Organizations**.
The project compares decision-making models in a repeated choice environment. The simulated setting is a cable car system where an agent repeatedly chooses between a safe option and a risky option.

## Project Idea

The passenger chooses between:
- **Safe choice (S):** board the first cable car that arrives.
- **Risky choice (R):** wait until an empty cable car arrives.

The environment creates a tradeoff between comfort and waiting time. The simulation compares how different behavioral decision rules perform over repeated trials.


## Models Compared

- Explorative agent
- Small recent sample agent
- Threshold agent
- Maximin agent

## Main Methods

- Repeated decision simulation
- Agent-based modeling
- Behavioral decision rules
- Batch simulation over many runs
- Reward comparison
- Risk-rate analysis
- Visual analysis of model behavior

## Tools

- Python
- NumPy
- pandas
- SciPy
- matplotlib
- seaborn


## Files

- `main.py` - runs the simulation
- `Agent.py` - defines the behavioral agents
- `cableCart.py` - defines the cable car environment
- `Simulator.py` - runs repeated simulations and exports results
- `VisualAnalysis.py` - creates graphs from the simulation results
- `report.pdf` - final report
- `requirements.txt` - required Python packages

## Outputs

The `export/` folder contains CSV files generated by the simulation.
The `plots/` folder contains visual outputs used to analyze the agents' behavior and compare their performance.


## How to Run

Install the required packages:
```bash
pip install -r requirements.txt
```
Run the simulation:
```bash
python main.py
```
Run the visual analysis:
```bash
python VisualAnalysis.py
```
